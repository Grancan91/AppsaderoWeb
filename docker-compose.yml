services:
  mysql:
    image: mysql:8.0
    container_name: appsadero-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-appsadero}
      MYSQL_USER: ${MYSQL_USER:-appsadero}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-appsadero}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./mysql-init:/docker-entrypoint-initdb.d
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-rootpassword}" ]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - learnapp-net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: learnapp-backend
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      NODE_ENV: development
      PORT: 3000
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: ${MYSQL_DATABASE:-learnapp}
      DB_USER: ${MYSQL_USER:-learnapp}
      DB_PASSWORD: ${MYSQL_PASSWORD:-learnapp}
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    command: npm run dev
    networks:
      - learnapp-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: learnapp-frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      VITE_API_URL: http://localhost:3000/api
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0
    networks:
      - learnapp-net

volumes:
  mysql_data:
    driver: local

networks:
  learnapp-net:
    driver: bridge
